// Path: backend/src/configs/index.js

const generalConfig = require('./general.config');
const dbConfig = require('./db.config');
const authConfig = require('./auth.config');


module.exports = {
    generalConfig,
    dbConfig,
    authConfig
};